# Final submission checklist

The Bradbury receipt run is complete and the public page uses its live report.
The remaining pre-submit checks are external verification steps, not missing
measurement data:

- confirm the `commit_report` transaction and add its explorer link if the
  report is anchored on-chain; until then, do not describe the report as
  committed on-chain;
- verify the production URL and the 40-line publish manifest point to the same
  contract and report hash;
- include the reel only after it has been rendered from the current numbers;
- do not describe the local corpus or challenge methods as deployed live
  measurements.
